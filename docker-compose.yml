version: '3.8'

services:
  course-central:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: course-central-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
